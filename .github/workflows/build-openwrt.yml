name: OpenWrt x86 All-In-One Firmware
on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 1 * *'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: 1986537311/openwrta
          ref: 25.12
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo apt update && sudo apt install -y build-essential flex bison git libssl-dev libelf-dev ncurses-dev zlib1g-dev gawk gettext unzip file lib32gcc-s1 python3

      - name: Setup feeds (修复 passwall 拉取问题)
        run: |
          cd $GITHUB_WORKSPACE
          # 先清空 feeds.conf.default
          > feeds.conf.default
          # 写入官方 feeds
          echo "src-git base https://git.openwrt.org/openwrt/openwrt.git;openwrt-25.12" >> feeds.conf.default
          echo "src-git packages https://git.openwrt.org/feed/packages.git;openwrt-25.12" >> feeds.conf.default
          echo "src-git luci https://git.openwrt.org/project/luci.git;openwrt-25.12" >> feeds.conf.default
          echo "src-git routing https://git.openwrt.org/feed/routing.git;openwrt-25.12" >> feeds.conf.default
          echo "src-git telephony https://git.openwrt.org/feed/telephony.git;openwrt-25.12" >> feeds.conf.default
          # 写入第三方 feeds（使用稳定镜像源）
          echo "src-git helloworld https://github.com/fw876/helloworld.git;main" >> feeds.conf.default
          echo "src-git openclash https://github.com/vernesong/OpenClash.git;master" >> feeds.conf.default
          echo "src-git small https://github.com/kenzok8/small.git" >> feeds.conf.default
          echo "src-git smallpackage https://github.com/kenzok8/small-package.git" >> feeds.conf.default
          # 增加重试机制拉取 feeds
          ./scripts/feeds update -a || ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Configure firmware (x86 64 + 全家桶)
        run: |
          cd $GITHUB_WORKSPACE
          cat > .config << 'EOF'
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_TARGET_ROOTFS_PARTSIZE=512
          CONFIG_TARGET_IMAGES_GZIP=y
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-theme-argon=y

          # 商店全家桶
          CONFIG_PACKAGE_luci-app-opkg=y
          CONFIG_PACKAGE_luci-app-store=y
          CONFIG_PACKAGE_luci-i18n-luci-app-store-zh-cn=y

          # Docker全家桶
          CONFIG_PACKAGE_docker=y
          CONFIG_PACKAGE_dockerd=y
          CONFIG_PACKAGE_containerd=y
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y

          # 游戏防封全家桶
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-i18n-openclash-zh-cn=y
          CONFIG_PACKAGE_luci-app-ssr-plus=y
          CONFIG_PACKAGE_luci-i18n-ssr-plus-zh-cn=y
          CONFIG_PACKAGE_kmod-tcp-bbr=y
          CONFIG_PACKAGE_fullcone=y

          # 手机备份全家桶
          CONFIG_PACKAGE_luci-app-fileassistant=y
          CONFIG_PACKAGE_luci-app-cloudreve=y
          CONFIG_PACKAGE_luci-app-aria2=y
          CONFIG_PACKAGE_luci-i18n-aria2-zh-cn=y
          CONFIG_PACKAGE_luci-app-syncthing=y
          CONFIG_PACKAGE_luci-i18n-syncthing-zh-cn=y
          CONFIG_PACKAGE_luci-app-samba4=y
          CONFIG_PACKAGE_samba4-server=y

          # 系统工具
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-cpufreq=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-adguardhome=y
          EOF
          ./scripts/feeds install -a
          make defconfig
          make -j$(nproc) download

      - name: Compile firmware
        run: |
          cd $GITHUB_WORKSPACE
          make -j$(nproc) V=s

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: OpenWrt-x86-AllInOne
          path: bin/targets/x86/64/openwrt-x86_64-combined-squashfs.img.gz
          retention-days: 30
